# `@finografic/prettier-config`

> Custom finografic [Prettier](https://prettier.io) configuration.

## Usage

**Install**:

```bash
$ yarn add --dev @finografic/prettier-config
```

**Edit `package.json`**:

```jsonc
{
    // ...
    "prettier": "@finografic/prettier-config"
}
```
